# 預覽

這裡是《廢塔一夜》的可玩版本：引擎、介面和第一期劇情都在這個資料夾。它不是 GitHub Pages 的網站根目錄，也不會取代 `https://belinchan1001.github.io/wasted-tower/`。

倉庫根目錄的 `index.html` 必須與 `main` 相同。不要把這裡的檔案複製回去蓋掉它。

從倉庫根目錄啟動靜態伺服器後，打開 `/preview/`。也可以直接用瀏覽器打開這個資料夾裡的 `index.html`。
