# 預覽

這裡是《廢塔一夜》目前分支的靜態副本，用來在合併前看引擎。它不是 GitHub Pages 的網站根目錄，也不會取代 `https://belinchan1001.github.io/wasted-tower/`。

從倉庫根目錄啟動靜態伺服器後，打開 `/preview/`。更新玩法檔案之後，在倉庫根目錄執行：

```bash
node scripts/build-preview.js
```
